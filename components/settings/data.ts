// mockWorkspace segue em uso só pelo painel de LGPD (exclusão/exportação),
// que ainda não foi conectado ao backend — chega no M15.
export const mockWorkspace = {
  name: "Estúdio Vértice",
  logoUrl: null as string | null,
};
